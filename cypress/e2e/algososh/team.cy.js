describe('МБОУ АЛГОСОШ', () => {
    beforeEach(() => {

        cy.visit('http://localhost:3000/');
      })
      it('displays http://localhost:3000/', () => {
  
        // cy.get('.todo-list li').should('have.length', 2)
    


      })

  });